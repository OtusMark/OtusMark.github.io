import styled from "styled-components/macro";

export const MontB3Bold = styled.p`
  font-family: ${({theme}) => theme.font.family.montserrat};
  font-size: ${({theme}) => theme.font.size.b3};
  font-weight: 700;
`